{% extends 'layout.html.twig' %}

{% block body %}
    <h1>ShieldSSO</h1>
    <p>Single Sign On</p>
    {% if is_granted('ROLE_USER') %}
        <a href="{{ path('logout') }}">Logout</a>
    {% else %}
        <a href="{{ path('login') }}">Login</a>
    {% endif %}
{% endblock %}
